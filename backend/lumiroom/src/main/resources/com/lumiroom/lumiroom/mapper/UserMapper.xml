<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lumiroom.lumiroom.mapper.UserMapper">

  <select id="findUserByUsernameOrEmail" resultType="com.lumiroom.lumiroom.model.commons.User" parameterType="String">
    SELECT id, username, onboarding_complete, avatar_id, created_at, email, password_hash
    FROM users
    WHERE username = #{identifier} OR email = #{identifier}
    LIMIT 1
  </select>

  <select id="findUserById" resultType="com.lumiroom.lumiroom.model.commons.User" parameterType="String">
    SELECT id, username, onboarding_complete, avatar_id, created_at, email, password_hash
    FROM users
    WHERE id = #{id}::uuid
    LIMIT 1
  </select>

  <!-- this needs to return something, insert cannot return a model class -->
  <select id="insertUser" parameterType="com.lumiroom.lumiroom.model.commons.User"> 
    INSERT INTO users (
      username, onboarding_complete, avatar_id, email, password_hash
    )
    VALUES (
      #{username}, #{onboardingComplete}, #{avatarId}, #{email}, #{passwordHash}
    )
    RETURNING 
      id, username, onboarding_complete, avatar_id, created_at, email, password_hash
  </select>

</mapper>
